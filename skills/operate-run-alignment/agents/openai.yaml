interface:
  display_name: "Run Alignment"
  short_description: "Produce shared two-way alignment evidence"
  default_prompt: "Use $operate-run-alignment to produce a shared ServingStudioSim and vLLM or SGLang alignment whose evidence can validate the simulator or guide framework optimization."
