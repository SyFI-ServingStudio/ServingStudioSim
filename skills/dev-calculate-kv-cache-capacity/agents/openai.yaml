interface:
  display_name: "Calculate KV Cache Capacity"
  short_description: "Estimate KV cache memory and tokens"
  default_prompt: "Use $dev-calculate-kv-cache-capacity to estimate KV cache size and token capacity for a model."
