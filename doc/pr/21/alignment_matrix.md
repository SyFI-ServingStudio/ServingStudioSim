# glm52_nvfp4_b200 — case matrix

Generated by `python -m launcher alignment-campaign compare --markdown`.
Every number is read from a completed Analyzer report by the formula table in
`launcher/alignment_campaign/metrics.py`; none is transcribed by hand.

| Case | kernel_abs_pct | kernel_signed_pct | map_coverage_pct | e2e_mean_pct | output_tps_pct | server_tpot_mean_pct | server_ttft_mean_pct |
|---|---:|---:|---:|---:|---:|---:|---:|
| ep4_tp4_dp1/01_micro_throughput_c256 | +2.62% | +1.09% | 98.10% | -4.26% | +4.53% | -0.96% | +6.01% |
| ep4_tp4_dp1/02_graph_boundaries_c128 | +3.76% | +2.59% | 98.26% | -3.32% | +3.82% | +0.63% | -2.28% |
| ep4_tp4_dp1/03_balanced_anchor_c32 | +4.78% | +2.58% | 98.44% | -1.68% | +1.81% | -0.53% | -1.86% |
| ep4_tp4_dp1/04_prefill_spectrum_c32 | +3.04% | -1.44% | 98.97% | -3.93% | +4.01% | -2.09% | -3.56% |
| ep4_tp4_dp1/05_decode_spectrum_c64 | +0.77% | +0.40% | 98.35% | -2.63% | +1.00% | -2.95% | -7.48% |
| ep4_tp4_dp1/06_long_long_c8 | +12.94% | +12.33% | 98.03% | +4.59% | -4.34% | +4.68% | -1.78% |
| ep4_tp4_dp1/07_quadrant_interference_c48 | +3.11% | +0.02% | 98.72% | -1.74% | +1.31% | -2.21% | -2.66% |
| ep4_tp4_dp1/08_completion_churn_c96 | +1.13% | -0.12% | 98.45% | -0.18% | -1.18% | -2.20% | +2.09% |
| ep4_tp4_dp1/09_rate_knee | +3.93% | +3.93% | 97.86% | -4.28% | +0.11% | -4.18% | -11.14% |
| ep4_tp4_dp1/10_full_saturation_rate | +3.63% | -1.16% | 99.01% | -8.46% | +13.86% | -5.18% | -4.89% |
| ep4_tp4_dp1/11_long_prefill_ladder_c1 | +3.85% | -2.22% | 97.83% | +3.42% | -3.30% | +5.91% | +3.58% |
| ep4_tp4_dp1/12_ultra_context_decode_c1 | +1.13% | -0.05% | 99.34% | +6.99% | -6.54% | +10.63% | +2.89% |
| ep4_tp4_dp1/13_generation_length_matrix_c8 | +2.97% | -1.50% | 98.30% | +6.92% | -7.37% | +6.76% | -20.77% |
| ep4_tp4_dp1/14_near_full_long_decode_c21 | +3.93% | -2.25% | 99.18% | -2.33% | +1.93% | -2.37% | +6.25% |
| ep4_tp4_dp1/15_kv_overcommit_mixed_churn_c14 | +2.58% | -2.22% | 99.23% | +3.72% | -4.18% | +3.24% | +35.14% |

## Declared exceptions

- `ep4_tp4_dp1/01_micro_throughput_c256` **decode_batch_mean_pct** +2.76% (tolerance 3): Concurrency 256: measured 2.69-2.77% iteration-population divergence. Kernel absolute error is 2.62%, so the timing model is not implicated.
- `ep4_tp4_dp1/01_micro_throughput_c256` **iterations_pct** -2.69% (tolerance 3): Concurrency 256: measured 2.69-2.77% iteration-population divergence. Kernel absolute error is 2.62%, so the timing model is not implicated.
- `ep4_tp4_dp1/01_micro_throughput_c256` **prefill_tokens_mean_pct** +2.76% (tolerance 3): Concurrency 256: measured 2.69-2.77% iteration-population divergence. Kernel absolute error is 2.62%, so the timing model is not implicated.
- `ep4_tp4_dp1/01_micro_throughput_c256` **scheduled_kv_mean_pct** +2.76% (tolerance 3): Concurrency 256: measured 2.69-2.77% iteration-population divergence. Kernel absolute error is 2.62%, so the timing model is not implicated.
- `ep4_tp4_dp1/02_graph_boundaries_c128` **decode_batch_mean_pct** +3.00% (tolerance 3.2): Concurrency 128: measured 2.92-3.00% iteration-population divergence, the same scheduler-contention effect as case 01.
- `ep4_tp4_dp1/02_graph_boundaries_c128` **iterations_pct** -2.92% (tolerance 3.2): Concurrency 128: measured 2.92-3.00% iteration-population divergence, the same scheduler-contention effect as case 01.
- `ep4_tp4_dp1/02_graph_boundaries_c128` **prefill_tokens_mean_pct** +3.00% (tolerance 3.2): Concurrency 128: measured 2.92-3.00% iteration-population divergence, the same scheduler-contention effect as case 01.
- `ep4_tp4_dp1/02_graph_boundaries_c128` **scheduled_kv_mean_pct** +3.00% (tolerance 3.2): Concurrency 128: measured 2.92-3.00% iteration-population divergence, the same scheduler-contention effect as case 01.
- `ep4_tp4_dp1/04_prefill_spectrum_c32` **decode_batch_mean_pct** +1.27% (tolerance 1.4): Concurrency 32 across the 2048-token chunk boundary: measured 1.25-1.29%. The chunk boundary makes admission decisions differ by one chunk more often than the neighbouring c32 case 03, which stays under 0.55%.
- `ep4_tp4_dp1/04_prefill_spectrum_c32` **iterations_pct** -1.25% (tolerance 1.4): Concurrency 32 across the 2048-token chunk boundary: measured 1.25-1.29%. The chunk boundary makes admission decisions differ by one chunk more often than the neighbouring c32 case 03, which stays under 0.55%.
- `ep4_tp4_dp1/04_prefill_spectrum_c32` **prefill_tokens_mean_pct** +1.27% (tolerance 1.4): Concurrency 32 across the 2048-token chunk boundary: measured 1.25-1.29%. The chunk boundary makes admission decisions differ by one chunk more often than the neighbouring c32 case 03, which stays under 0.55%.
- `ep4_tp4_dp1/04_prefill_spectrum_c32` **scheduled_kv_mean_pct** +1.29% (tolerance 1.4): Concurrency 32 across the 2048-token chunk boundary: measured 1.25-1.29%. The chunk boundary makes admission decisions differ by one chunk more often than the neighbouring c32 case 03, which stays under 0.55%.
- `ep4_tp4_dp1/06_long_long_c8` **kernel_abs_pct** +12.94% (tolerance 13.5): Known long-prefill-plus-long-decode exception, 12.94% at acceptance. TODO.md item 1 (prefill kernel issues and timing placeholders) tracks it; the case is kept in the matrix precisely because it is the one that fails.
- `ep4_tp4_dp1/06_long_long_c8` **kernel_signed_pct** +12.33% (tolerance 13.5): Known long-prefill-plus-long-decode exception, 12.94% at acceptance. TODO.md item 1 (prefill kernel issues and timing placeholders) tracks it; the case is kept in the matrix precisely because it is the one that fails.
- `ep4_tp4_dp1/09_rate_knee` **decode_batch_mean_pct** -3.38% (tolerance 3.7): Open-loop arrival at the queueing knee: measured 3.38-3.49%. This case also still runs on a provisional rate (see campaign.yaml), so the exact operating point is not yet the calibrated knee.
- `ep4_tp4_dp1/09_rate_knee` **iterations_pct** +3.49% (tolerance 3.7): Open-loop arrival at the queueing knee: measured 3.38-3.49%. This case also still runs on a provisional rate (see campaign.yaml), so the exact operating point is not yet the calibrated knee.
- `ep4_tp4_dp1/09_rate_knee` **prefill_tokens_mean_pct** -3.38% (tolerance 3.7): Open-loop arrival at the queueing knee: measured 3.38-3.49%. This case also still runs on a provisional rate (see campaign.yaml), so the exact operating point is not yet the calibrated knee.
- `ep4_tp4_dp1/09_rate_knee` **scheduled_kv_mean_pct** -3.38% (tolerance 3.7): Open-loop arrival at the queueing knee: measured 3.38-3.49%. This case also still runs on a provisional rate (see campaign.yaml), so the exact operating point is not yet the calibrated knee.
- `ep4_tp4_dp1/10_full_saturation_rate` **decode_batch_mean_pct** +13.53% (tolerance 14): One finding with two faces. Throughput runs +13.86% while kernel absolute error is only 3.63%, and the iteration population diverges by 11.92-13.54% — the matrix's worst. At saturation the simulator admits work the framework does not, so it completes more tokens in fewer, fuller iterations. A clean tight level under a dirty loose one points at scheduling and admission dynamics, not at kernel cost, so the kernel bound is not relaxed with these. Tracing this back to admission policy is open work; these bounds keep it visible, they do not accept it.
- `ep4_tp4_dp1/10_full_saturation_rate` **e2e_mean_pct** -8.46% (tolerance 9): One finding with two faces. Throughput runs +13.86% while kernel absolute error is only 3.63%, and the iteration population diverges by 11.92-13.54% — the matrix's worst. At saturation the simulator admits work the framework does not, so it completes more tokens in fewer, fuller iterations. A clean tight level under a dirty loose one points at scheduling and admission dynamics, not at kernel cost, so the kernel bound is not relaxed with these. Tracing this back to admission policy is open work; these bounds keep it visible, they do not accept it.
- `ep4_tp4_dp1/10_full_saturation_rate` **iterations_pct** -11.92% (tolerance 14): One finding with two faces. Throughput runs +13.86% while kernel absolute error is only 3.63%, and the iteration population diverges by 11.92-13.54% — the matrix's worst. At saturation the simulator admits work the framework does not, so it completes more tokens in fewer, fuller iterations. A clean tight level under a dirty loose one points at scheduling and admission dynamics, not at kernel cost, so the kernel bound is not relaxed with these. Tracing this back to admission policy is open work; these bounds keep it visible, they do not accept it.
- `ep4_tp4_dp1/10_full_saturation_rate` **output_tps_pct** +13.86% (tolerance 15): One finding with two faces. Throughput runs +13.86% while kernel absolute error is only 3.63%, and the iteration population diverges by 11.92-13.54% — the matrix's worst. At saturation the simulator admits work the framework does not, so it completes more tokens in fewer, fuller iterations. A clean tight level under a dirty loose one points at scheduling and admission dynamics, not at kernel cost, so the kernel bound is not relaxed with these. Tracing this back to admission policy is open work; these bounds keep it visible, they do not accept it.
- `ep4_tp4_dp1/10_full_saturation_rate` **prefill_tokens_mean_pct** +13.53% (tolerance 14): One finding with two faces. Throughput runs +13.86% while kernel absolute error is only 3.63%, and the iteration population diverges by 11.92-13.54% — the matrix's worst. At saturation the simulator admits work the framework does not, so it completes more tokens in fewer, fuller iterations. A clean tight level under a dirty loose one points at scheduling and admission dynamics, not at kernel cost, so the kernel bound is not relaxed with these. Tracing this back to admission policy is open work; these bounds keep it visible, they do not accept it.
- `ep4_tp4_dp1/10_full_saturation_rate` **scheduled_kv_mean_pct** +13.53% (tolerance 14): One finding with two faces. Throughput runs +13.86% while kernel absolute error is only 3.63%, and the iteration population diverges by 11.92-13.54% — the matrix's worst. At saturation the simulator admits work the framework does not, so it completes more tokens in fewer, fuller iterations. A clean tight level under a dirty loose one points at scheduling and admission dynamics, not at kernel cost, so the kernel bound is not relaxed with these. Tracing this back to admission policy is open work; these bounds keep it visible, they do not accept it.
- `ep4_tp4_dp1/13_generation_length_matrix_c8` **server_ttft_mean_pct** -20.77% (tolerance 22): -20.77% is concentrated in the initial eight-request startup cohort; over requests 8-23 it is -2.60%. A startup-queue effect, not a per-request cost miss, and the full-run E2E mean stays inside the default bound.
- `ep4_tp4_dp1/15_kv_overcommit_mixed_churn_c14` **server_ttft_mean_pct** +35.14% (tolerance 36): +35.14% is concentrated in the initial fourteen-request KV-overcommit admission cohort; over requests 14-71 it is -6.34%. An admission-tail effect; the full-run E2E mean stays inside the default bound.

