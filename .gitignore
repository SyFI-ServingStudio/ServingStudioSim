# Rust
/target/
**/*.rs.bk
Cargo.lock

# Python
__pycache__/
*.py[cod]
.venv/
.uv/

# Data
*.parquet
*.pftrace
profile.db-journal
logs/
# Large workload trace: provisioned per-worktree, not tracked (see create-worktree)
trace/aime_long.csv
# 5.2 MB tracegen output — regenerate with TraceLab rather than carry it in history
trace/tracelab_preserving.csv

# Editor / OS
.vscode/
.idea/
.DS_Store

# Agent runtime state (per-machine; .claude/ and .codex/ skills symlinks
# are tracked, local settings files are not)
.claude/settings.local.json
.codex/*.local.*

# Generated docs / sample output — regenerable, not source of truth
.code-lessons/
model/work/examples/

/tmp/
